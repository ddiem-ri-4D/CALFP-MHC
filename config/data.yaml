# =============================================================================
# CALFP Dataset and Results Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Input data files
# -----------------------------------------------------------------------------
mhc_seq: data/pseudosequence.2016.all.X.dat   # MHC pseudosequences file
train: data/data2.txt                         # Training dataset for fine-tuning
pretrain: data/data_pretrain2.txt             # Pretraining dataset
cv_id: data/cv_id2.txt                        # Cross-validation split IDs
binding: data/binding.txt                     # Binding core dataset for evaluation

# -----------------------------------------------------------------------------
# Output directories
# -----------------------------------------------------------------------------
results: results                              # Directory for saving results and outputs
